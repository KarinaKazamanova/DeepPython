
import csv
import json
import os
import pickle



def directory_info(result_dict, path):
    directory = os.path.join(os.getcwd())
    if path == directory:
        return result_dict
    else:
        if 
